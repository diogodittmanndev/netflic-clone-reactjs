import styled from "styled-components";

export const FeaturedMovie = styled.section`
    background-size: cover;
    background-position: center;
    height: 100vh;
`;

export const FeaturedEffectBottom = styled.div`
    width: inherit;
    height: inherit;
    background: linear-gradient(to top, #111 10%, transparent 90%);
`;

export const FeaturedEffectLeft = styled.div`
    width: inherit;
    height: inherit;
    background: linear-gradient(to right, #111 25%, transparent 75%);
`;

export const FeaturedMovieTitle = styled.div``;
